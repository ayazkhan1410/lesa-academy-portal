# Copy to `.env.local` for local dev (optional — defaults to http://127.0.0.1:8000)
VITE_API_BASE_URL=http://127.0.0.1:8000

# Vercel production example (set in Vercel Project → Settings → Environment Variables)
# VITE_API_BASE_URL=https://your-django-api.example.com
